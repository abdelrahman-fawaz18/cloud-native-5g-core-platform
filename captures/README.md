# Packet Evidence

Packet captures are ignored by default. Only narrowly collected, intentionally
reviewed, synthetic protocol evidence may be added. Prefer tshark summaries or
small fixtures when a complete capture is unnecessary.

Each published item must document capture point, filter, protocol scope,
privacy review, and the technical claim it supports.

# Centralized Logging

This directory will contain the accepted lightweight logging stack and its
configuration. Logs must preserve component and time context while preventing
secrets, raw subscriber credentials, and unrelated host data from entering
published evidence.

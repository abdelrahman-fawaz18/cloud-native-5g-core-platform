# Repository Automation

GitHub Actions validates documentation, shell, Python, YAML,
container definitions, Helm charts, Kubernetes schemas/policies, dependencies,
secrets, and release artifacts.

Privileged 5G end-to-end tests must be separated from ordinary hosted-runner
checks and must never be represented as executed when the runner lacks SCTP,
TUN, GTP-U, or required capabilities.

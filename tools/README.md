# Validation And Reporting Tools

This directory will contain software that generates subscriber/configuration
data, validates 5G and Kubernetes state, runs controlled tests, correlates
evidence, and renders machine-readable and Markdown reports.

Tools must distinguish platform readiness, 5G signalling success, PDU-session
success, and user-plane connectivity.
